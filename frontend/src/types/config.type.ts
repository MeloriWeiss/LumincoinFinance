export type ConfigType = {
    api: string,
    date: {
        today: string,
        week: string,
        month: string,
        year: string,
        all: string,
        interval: string,
    }
}